h2 Write a new post
label(for='title') Title: 
input(ng-model='form.title', name='title')
p Body:
form(role='form')
  div.form-group
    textarea.form-control(ng-model='form.text', cols='50', rows='15')
p
  button.btn-success(ng-click='submitPost()') Submit Post