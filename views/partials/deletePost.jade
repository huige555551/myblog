h2 {{post.title}}
div {{post.text}}
p
  | Are you sure you want to delete this post? 
  button.btn.btn-success(ng-click='deletePost()') Yes
  |  - 
  button.btn.btn-danger(ng-click='home()') No thanks